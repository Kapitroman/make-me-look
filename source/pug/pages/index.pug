extends ../components/base/layout

block variables
  - const title = "MakeMeLook" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = false
  - const headerMods = ""
  - const hasFooter = false
  - const footerMods = ""
  - const currentUrl = "index.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main.wrapper
    //- на странице запрещен "голый" код
    //- все блоки вставляются из папки pug/components/blocks
    //- +intro()
    section.wrapper__panel.blue
      +intro()
    section.wrapper__panel.red
      +intro()
    section.wrapper__panel.orange
      +intro()
    section.wrapper__panel.purple
      +intro()


block modals
